# Context for use with Claude code

# Workflow
- Always end by running tests and making sure none fail
- on `mix compile --force` we must not see any warnings
- on `mix test` we must also see no warnings
