# .dialyzer_ignore.exs
#
# Warnings listed here are suppressed by Dialyzer.
# To add a warning, run `mix dialyzer` and copy the exact warning string.
# Format: one string per line, each matching a Dialyzer warning.
#
# RATCHET RULE: Only add entries for pre-existing warnings.
# New code must pass Dialyzer clean.
[
  # Example (remove when real entries are added):
  # ~r/lib\/cake\/documents\/hexdocs\/downloads.ex.*Function clone_and_get_module_paths/
]
