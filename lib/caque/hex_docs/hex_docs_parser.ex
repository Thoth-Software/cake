###Html files have a are structured simply, they have a main div within the body
###the div has a button, nav, and main. Main is what we focus on.

###body -> div -> main -> div, within that skip the first div which is the search bar
#within that it's all h tags and p tags with some code, img, and a embeddings
